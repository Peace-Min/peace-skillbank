1. Port exactly ONE unit per request: the C# file(s) named in the prompt (a `partial` class arrives as several files that form one unit), into `<same path>.h` + `.cpp` (the file holding `Main` becomes a single `.cpp` with `wmain`). Nothing else.
2. Never invent a declaration. Use only: the C++ standard library, `<windows.h>`, `PortSupport.h`, and the "Declarations you may use" section. If a needed member is missing there, write `// TODO(port): needs <what>` at the use site and keep going.
3. Never write C++/CLI or .NET-flavoured C++: no `System::`, no `gcnew`, no `^` handles, no `String^`, no `using namespace System`, no `#include <System...>`, no `#using`, no `Console::WriteLine`.
4. Strings are `std::wstring` with `L"..."` literals. `std::string` appears only where `PortSupport::ToUtf8` / `FromUtf8` cross a byte boundary, and in exception messages (narrow ASCII literals only).
5. No raw owning pointers: no `new`, no `delete`, no `malloc`. Ownership follows the "Ownership (computed)" list in the prompt: SHARED types are `std::shared_ptr` (`std::make_shared`), SINGLE types are plain values (or `std::unique_ptr` when polymorphic); structs and strings by value.
6. Keep the C# structure recognisable: same names (PascalCase methods; properties become `Name()` / `SetName(v)`), same member order, same behaviour, same output text byte-for-byte.
7. Header: `#pragma once`, includes, namespace, class declaration only (trivial inline getters are fine). Source: definitions. Modern C++ idioms from the mapping table are mandatory: `[[nodiscard]]` getters, `noexcept` destructors, `override`/`final`, `= default`/`= delete`, `constexpr` constants, `auto` locals, range-for, `static_cast`, `nullptr`, `using` aliases.
8. Include what you use, relative to the C++ root (`#include "Models/Device.h"`); never `..` paths; include `"PortSupport.h"` when you use it.
9. Where C# behaviour cannot be reproduced exactly (decimal precision, dictionary order, finalizers, reflection, threading) keep the closest mapped equivalent and mark it `// TODO(port): <difference>`.
10. Every function body in full. No `// ...`, no "rest unchanged", no "omitted", no partial files.
11. When a build error list is included in the prompt, fix those errors and change nothing else. If an error is inside a file you were told not to edit, do not edit it: add `// TODO(port): blocked by <file>:<line>` and report it.
