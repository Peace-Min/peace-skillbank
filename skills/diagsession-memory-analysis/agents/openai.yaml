interface:
  display_name: "DiagSession Memory Analysis"
  short_description: "Prepare VS gcdump snapshots for managed leak analysis."
  brand_color: "#2563EB"
  default_prompt: "Use $diagsession-memory-analysis to extract gcdump reports from my Visual Studio diagsession snapshots and analyze the managed memory leak evidence."

policy:
  allow_implicit_invocation: true
