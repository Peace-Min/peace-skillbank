{
  "id": "5001", "checker": "FORWARD_NULL", "file": "Foo.cs", "line": "88",
  "verdict": "진성",
  "rationale": "가이드 진성 판별 기준: FirstOrDefault 등 null 반환 가능 API 결과를 무검사 역참조(node.Value). 87행에 가드가 없어 node 가 null 이면 NRE.",
  "fix": {
    "lines": "86-88",
    "before": "var node = list.FirstOrDefault(n => n.Id == id);\nProcess(node.Value);",
    "after": "var node = list.FirstOrDefault(n => n.Id == id);\nif (node == null) return;\nProcess(node.Value);"
  },
  "false_positive_reason": "",
  "hold_reason": "",
  "needs_context": false,
  "missing_context": [],
  "needs_frontier": false,
  "cwe": "CWE-476", "weapon_item": "미매핑(187 추출 후 기입)"
}
